// Touch & Choose runs entirely in the browser: it speaks, goes fullscreen and
// plays media, so there is nothing useful to render on a server.
export const ssr = false;
export const prerender = false;
